services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: nevera_user
      POSTGRES_PASSWORD: nevera_pass
      POSTGRES_DB: nevera_db
    ports:
      - "5432:5432"
    volumes:
      - nevera_pgdata:/var/lib/postgresql/data

volumes:
  nevera_pgdata: